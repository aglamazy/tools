// Extension config — set via NEXT_PUBLIC_SITE_URL in .env.local
// For dev: http://localhost:3100
// For prod: https://tools.aglamaz.com (or new domain)
// Change this value and reload the extension to switch environments.
const SITE_URL = 'https://tools.aglamaz.com';
