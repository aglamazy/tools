# 2026-09-11

## What was worked on

- **aglamazo#363/#365 (Sheli's morning bug reports):** fixed cross-file import dedup gap
  (Isracard xlsx truncates merchant names — `merchantsMatchForDedup` in `dedupKey.ts`), and
  closed the root cause of a 2026-07-13/14 supplier-seed race (`supplierMigration.ts` now
  wraps the whole seed in one Dexie transaction so the browser's own IndexedDB engine
  serializes it cross-tab). aglamazo#362 (false import-gap month) investigated but not
  root-caused — `getImportedFiles()` cleared by Sheli's live data pull, actual cause unknown.
- **aglamazo#364/#365 cleanup, authorized by Agla ("go", msg #44942):** built
  `duplicateCleanupService.ts` (mergeDuplicateSuppliers, mergeSupplierEmptyEmailDuplicates,
  deleteCategoryById, deleteTransactionById) and wired three separately-confirmed admin
  controls into Settings → Advanced. Split the still-open תשתיות category decision (general#48)
  into its own confirm box per Sheli's correction — never bundle an undecided action with an
  authorized one. Agla ran it live: 1,176 excess supplier rows merged, stray tx 2081 removed,
  תשתיות duplicate resolved, tombstones verified intact. Built and shipped a second-pass merge
  for the 31 rows the strict-match pass left behind (empty-emailSenders subset rule, Sheli's
  spec). Filed aglamazo#367 (closed, superseded by #366) for the dead `db.categories` table
  Sheli spotted as a landmine.
- **Sheli/Tester's live bug report (aglamazo#368):** fixed a real bidi digit-reversal bug
  (Latin digits inside RTL Hebrew vendor names rendered visually reversed — wrapped 4 render
  sites in `<bdi>`) and the mechanism behind /app/budget rendering permanently blank after a
  load error (uncaught throw in the loading effect left `loading` stuck true forever with no
  error surfaced — added try/catch/finally + a visible error banner). Root upstream throw
  itself not pinned down from static reading; the fix makes it visible next time.
- **Sheli standing-agentic-access scoping (General/Agla, paused mid-scope):** researched and
  drafted `docs/15-sheli-agentic-access-scoping.md` covering the relay pattern, capability-
  scoped action surface, and Agla's verb-layer correction ("one canonical function per
  business action, UI and agent are two callers of the same thing"). Grounded against
  Compass's identity-delegation investigation and the fleet's rung 1-3 ladder (nothing built
  yet). General called a hold mid-draft — still in planning with Agla, real ask not sent yet.
  Left as an uncommitted local draft, not sent anywhere.

## Key decisions

- Never bundle a still-open decision (תשתיות category delete, general#48) into the same
  confirm control as an already-authorized action — Sheli's correction, now the pattern for
  all future admin-cleanup UI in this app.
- Agla clicks destructive one-time admin actions; no agent (me or Sheli) drives them, even
  via console/devtools — Buddy's standing ruling on agent-driven browser writes.
- The Sheli-agentic-access feature's identity/delegation layer is explicitly NOT Aglamazo's
  to design solo — depends on the fleet capability spine (compass#77/#78), which doesn't
  exist yet. Scoping stayed inside what Aglamazo actually owns (relay, verb layer, audit).

## Loose ends

- aglamazo#362 (false import-gap) — root cause still unknown, `getImportedFiles()` cleared.
- aglamazo#366 — id-vs-name structural fix for subject/category collapse + the db.categories
  legacy-table question, needs a Sheli/Agla design decision before implementation. Not urgent.
- aglamazo#368's actual upstream throw in budget's loadData — not root-caused, only made
  visible. Watch for the new error banner text if it recurs.
- `docs/15-sheli-agentic-access-scoping.md` — uncommitted draft, paused by General mid-scope.
  Real ask not yet sent; don't build anything until it lands.
- Sheli flagged a structural QA gap (Aglamazo can only be visually tested on Agla's own
  machine) and proposed a seeded demo-mode route — explicitly not asking for a build yet,
  filed for the record, Agla's call.
- Reported a recurring Octopus Arm transport-drop pattern to Buddy — turned out to be a known,
  tracked mux-restart issue (bob#165), not Aglamazo-specific. No action needed on this side.
- `app/route-lastmod.json` (modified) and `poc/isracard-scraper` (untracked) were already
  dirty before this session started — not touched, left for the user to decide.
